# ------------------------------------------------------------------------------
# 核心依赖(必需)
# ------------------------------------------------------------------------------
flask>=3.0
numpy>=1.26

# ------------------------------------------------------------------------------
# 可选依赖: 安装后预测引擎自动启用 IsolationForest 异常检测与随机森林阶段
# 分类; 未安装时自动降级为 Mahalanobis 距离 + 规则引擎(输出接口完全一致)。
# 需要时取消下面一行的注释或手动安装:
# ------------------------------------------------------------------------------
# scikit-learn>=1.3
